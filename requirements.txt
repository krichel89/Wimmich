# Obergrenzen mit Absicht. Bei Cammello hat eine 18 Stunden zuvor
# veröffentlichte Abhängigkeit den Windows-Build zerlegt, während Mac und
# Linux durchliefen. Ohne Deckel kann jede beliebige Veröffentlichung
# einen Build über Nacht kippen, der gestern noch lief.
PyQt6>=6.6,<7
Pillow>=10.0,<12
rawpy>=0.19,<0.26
numpy>=1.24,<3

# Optional: verbessert das Füllen von Rissen (Telea-Verfahren).
# Ohne OpenCV läuft alles weiter, nur die Rissreparatur ist einfacher.
# Achtung: bläht ein gebautes Paket um rund 195 MB auf.
# opencv-python>=4.9,<5
